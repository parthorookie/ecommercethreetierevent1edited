name: CD - Terraform Infra + ArgoCD GitOps on AWS EKS

on:
  push:
    branches: [main]
    paths:
      - "terraform/**"
      - "helm/**"
      - "argo/**"
      - "k8s/**"
  workflow_dispatch:
    inputs:
      action:
        description: "Terraform action"
        required: true
        default: "apply"
        type: choice
        options: [plan, apply, destroy]

env:
  AWS_REGION:       ap-south-1
  TF_DIR:           terraform
  EKS_CLUSTER_NAME: ecommerce-eks
  GITHUB_REPO:      https://github.com/parthorookie/ecommercethreetierevent1edited

jobs:

  # ═══════════════════════════════════════════════════════════════════════════
  # JOB 1 — Bootstrap S3 state bucket
  # ═══════════════════════════════════════════════════════════════════════════
  bootstrap-state:
    name: Bootstrap S3 State Bucket
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch'
    steps:
      - uses: actions/checkout@v4
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id:     ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region:            ${{ env.AWS_REGION }}
      - name: Create S3 bucket with native Object Lock
        run: |
          BUCKET="ecommerce-terraform-state-prod"
          REGION="${{ env.AWS_REGION }}"
          if aws s3api head-bucket --bucket "$BUCKET" 2>/dev/null; then
            echo "Bucket $BUCKET already exists"
          else
            aws s3api create-bucket \
              --bucket "$BUCKET" --region "$REGION" \
              --create-bucket-configuration LocationConstraint="$REGION" \
              --object-lock-enabled-for-bucket
            aws s3api put-bucket-versioning \
              --bucket "$BUCKET" \
              --versioning-configuration Status=Enabled
            aws s3api put-object-lock-configuration \
              --bucket "$BUCKET" \
              --object-lock-configuration \
              '{"ObjectLockEnabled":"Enabled","Rule":{"DefaultRetention":{"Mode":"GOVERNANCE","Days":30}}}'
            aws s3api put-bucket-encryption \
              --bucket "$BUCKET" \
              --server-side-encryption-configuration \
              '{"Rules":[{"ApplyServerSideEncryptionByDefault":{"SSEAlgorithm":"AES256"}}]}'
            aws s3api put-public-access-block \
              --bucket "$BUCKET" \
              --public-access-block-configuration \
              "BlockPublicAcls=true,IgnorePublicAcls=true,BlockPublicPolicy=true,RestrictPublicBuckets=true"
            echo "Bucket created with Object Lock GOVERNANCE 30 days"
          fi

  # ═══════════════════════════════════════════════════════════════════════════
  # JOB 2 — Terraform Plan
  # ═══════════════════════════════════════════════════════════════════════════
  terraform-plan:
    name: Terraform Plan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id:     ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region:            ${{ env.AWS_REGION }}
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.7.0"
      - name: Terraform Init
        working-directory: ${{ env.TF_DIR }}
        run: |
          terraform init \
            -backend-config="bucket=ecommerce-terraform-state-prod" \
            -backend-config="key=prod/terraform.tfstate" \
            -backend-config="region=${{ env.AWS_REGION }}" \
            -backend-config="encrypt=true" \
            -backend-config="use_lockfile=true"
      - name: Terraform Validate
        working-directory: ${{ env.TF_DIR }}
        run: terraform validate
      - name: Terraform Plan
        working-directory: ${{ env.TF_DIR }}
        run: |
          terraform plan \
            -var="region=${{ env.AWS_REGION }}" \
            -var="db_password=${{ secrets.DB_PASSWORD }}" \
            -var="project_name=ecommerce" \
            -var="environment=prod" \
            -out=tfplan -detailed-exitcode
        continue-on-error: true
      - name: Upload plan artifact
        uses: actions/upload-artifact@v4
        with:
          name: tfplan
          path: ${{ env.TF_DIR }}/tfplan
          retention-days: 1

  # ═══════════════════════════════════════════════════════════════════════════
  # JOB 3 — Terraform Apply
  # ═══════════════════════════════════════════════════════════════════════════
  terraform-apply:
    name: Terraform Apply
    runs-on: ubuntu-latest
    needs: terraform-plan
    if: github.ref == 'refs/heads/main' && (github.event.inputs.action == 'apply' || github.event_name == 'push')
    environment: production
    outputs:
      eks_cluster_name: ${{ steps.tf_out.outputs.eks_cluster_name }}
      rabbitmq_ip:      ${{ steps.tf_out.outputs.rabbitmq_ip }}
      aurora_endpoint:  ${{ steps.tf_out.outputs.aurora_endpoint }}
      alb_dns_name:     ${{ steps.tf_out.outputs.alb_dns_name }}
      ecr_backend_url:  ${{ steps.tf_out.outputs.ecr_backend_url }}
      ecr_worker_url:   ${{ steps.tf_out.outputs.ecr_worker_url }}
    steps:
      - uses: actions/checkout@v4
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id:     ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region:            ${{ env.AWS_REGION }}
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.7.0"
      - name: Terraform Init
        working-directory: ${{ env.TF_DIR }}
        run: |
          terraform init \
            -backend-config="bucket=ecommerce-terraform-state-prod" \
            -backend-config="key=prod/terraform.tfstate" \
            -backend-config="region=${{ env.AWS_REGION }}" \
            -backend-config="encrypt=true" \
            -backend-config="use_lockfile=true"
      - name: Download plan
        uses: actions/download-artifact@v4
        with:
          name: tfplan
          path: ${{ env.TF_DIR }}
      - name: Terraform Apply
        working-directory: ${{ env.TF_DIR }}
        run: terraform apply -auto-approve tfplan
      - name: Capture Terraform outputs
        id: tf_out
        working-directory: ${{ env.TF_DIR }}
        run: |
          echo "eks_cluster_name=$(terraform output -raw eks_cluster_name)"  >> $GITHUB_OUTPUT
          echo "rabbitmq_ip=$(terraform output -raw rabbitmq_private_ip)"    >> $GITHUB_OUTPUT
          echo "aurora_endpoint=$(terraform output -raw aurora_endpoint)"    >> $GITHUB_OUTPUT
          echo "alb_dns_name=$(terraform output -raw alb_dns_name)"          >> $GITHUB_OUTPUT
          echo "ecr_backend_url=$(terraform output -raw ecr_backend_url)"    >> $GITHUB_OUTPUT
          echo "ecr_worker_url=$(terraform output -raw ecr_worker_url)"      >> $GITHUB_OUTPUT

  # ═══════════════════════════════════════════════════════════════════════════
  # JOB 4 — Install ArgoCD on EKS exposed via AWS Network Load Balancer
  #
  # KEY DIFFERENCE from old version:
  #   OLD → server.service.type=ClusterIP  (only via port-forward)
  #   NEW → server.service.type=LoadBalancer (real public AWS NLB URL)
  #
  # After this job: open http://<NLB-hostname> in ANY browser = ArgoCD UI
  # ═══════════════════════════════════════════════════════════════════════════
  install-argocd:
    name: Install ArgoCD (AWS LoadBalancer)
    runs-on: ubuntu-latest
    needs: terraform-apply
    outputs:
      argocd_url:      ${{ steps.argocd_lb.outputs.argocd_url }}
      argocd_password: ${{ steps.argocd_pass.outputs.password }}
    steps:
      - uses: actions/checkout@v4
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id:     ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region:            ${{ env.AWS_REGION }}
      - uses: azure/setup-kubectl@v3
      - uses: azure/setup-helm@v4
      - name: Update kubeconfig
        run: |
          aws eks update-kubeconfig \
            --region ${{ env.AWS_REGION }} \
            --name ${{ needs.terraform-apply.outputs.eks_cluster_name }}
          kubectl get nodes
      - name: Create namespaces
        run: |
          for ns in argocd argo worker keda monitoring; do
            kubectl create namespace "$ns" --dry-run=client -o yaml | kubectl apply -f -
          done
      - name: Add Helm repos
        run: |
          helm repo add argo               https://argoproj.github.io/argo-helm
          helm repo add kedacore           https://kedacore.github.io/charts
          helm repo add prometheus-community https://prometheus-community.github.io/helm-charts
          helm repo update

      # ArgoCD installed with service.type=LoadBalancer
      # AWS automatically creates a Network Load Balancer and assigns a
      # public DNS hostname — no port-forward ever needed again
      - name: Install ArgoCD via Helm (service type LoadBalancer)
        run: |
          helm upgrade --install argocd argo/argo-cd \
            --namespace argocd \
            --version 6.7.3 \
            --set server.service.type=LoadBalancer \
            --set server.service.servicePortHttp=80 \
            --set server.extraArgs[0]="--insecure" \
            --set configs.params."server\.insecure"=true \
            --set "server.service.annotations.service\.beta\.kubernetes\.io/aws-load-balancer-type"=nlb \
            --set "server.service.annotations.service\.beta\.kubernetes\.io/aws-load-balancer-scheme"=internet-facing \
            --set "server.service.annotations.service\.beta\.kubernetes\.io/aws-load-balancer-cross-zone-load-balancing-enabled"=true \
            --wait --timeout=300s
          echo "ArgoCD installed with AWS NLB"

      - name: Wait for ArgoCD pods
        run: |
          kubectl rollout status deployment/argocd-server      -n argocd --timeout=180s
          kubectl rollout status deployment/argocd-repo-server -n argocd --timeout=180s
          kubectl rollout status statefulset/argocd-application-controller -n argocd --timeout=180s

      # Poll until AWS assigns the NLB hostname (takes ~2 min)
      - name: Get ArgoCD LoadBalancer hostname
        id: argocd_lb
        run: |
          echo "Waiting for AWS NLB to assign hostname..."
          for i in $(seq 1 24); do
            HOSTNAME=$(kubectl get svc argocd-server -n argocd \
              -o jsonpath='{.status.loadBalancer.ingress[0].hostname}' 2>/dev/null || echo "")
            if [ -n "$HOSTNAME" ] && [ "$HOSTNAME" != "null" ]; then
              echo "ArgoCD LB hostname: $HOSTNAME"
              echo "argocd_url=http://$HOSTNAME" >> $GITHUB_OUTPUT
              break
            fi
            echo "Attempt $i/24 — waiting 10s..."
            sleep 10
          done
          if [ -z "$HOSTNAME" ]; then
            kubectl describe svc argocd-server -n argocd
            exit 1
          fi

      - name: Get ArgoCD admin password
        id: argocd_pass
        run: |
          PASS=$(kubectl -n argocd get secret argocd-initial-admin-secret \
            -o jsonpath="{.data.password}" | base64 -d)
          echo "password=$PASS" >> $GITHUB_OUTPUT

      - name: Install Argo Workflows (LoadBalancer)
        run: |
          helm upgrade --install argo-workflows argo/argo-workflows \
            --namespace argo \
            --set workflow.serviceAccount.create=true \
            --set server.authModes="{server}" \
            --set server.serviceType=LoadBalancer \
            --set "server.serviceAnnotations.service\.beta\.kubernetes\.io/aws-load-balancer-type"=nlb \
            --set "server.serviceAnnotations.service\.beta\.kubernetes\.io/aws-load-balancer-scheme"=internet-facing \
            --wait --timeout=300s

      - name: Install KEDA
        run: |
          helm upgrade --install keda kedacore/keda \
            --namespace keda --wait --timeout=300s

      - name: Install kube-prometheus-stack (Grafana LoadBalancer)
        run: |
          helm upgrade --install prometheus prometheus-community/kube-prometheus-stack \
            --namespace monitoring \
            --set grafana.adminPassword="${{ secrets.GRAFANA_PASSWORD }}" \
            --set grafana.service.type=LoadBalancer \
            --set "grafana.service.annotations.service\.beta\.kubernetes\.io/aws-load-balancer-type"=nlb \
            --set "grafana.service.annotations.service\.beta\.kubernetes\.io/aws-load-balancer-scheme"=internet-facing \
            --set prometheus.prometheusSpec.serviceMonitorSelectorNilUsesHelmValues=false \
            --wait --timeout=600s

      - name: Print ArgoCD URL
        run: |
          echo "===================================================="
          echo " ArgoCD UI  : ${{ steps.argocd_lb.outputs.argocd_url }}"
          echo " Username   : admin"
          echo " Password   : ${{ steps.argocd_pass.outputs.password }}"
          echo "===================================================="

  # ═══════════════════════════════════════════════════════════════════════════
  # JOB 5 — Configure ArgoCD: register parthorookie/ecommercethreetierevent1edited
  #         and create Applications pointing to helm/ecommerce + helm/worker
  # ═══════════════════════════════════════════════════════════════════════════
  configure-argocd:
    name: Configure ArgoCD (register repo + create apps)
    runs-on: ubuntu-latest
    needs: [terraform-apply, install-argocd]
    steps:
      - uses: actions/checkout@v4
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id:     ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region:            ${{ env.AWS_REGION }}
      - uses: azure/setup-kubectl@v3
      - name: Update kubeconfig
        run: |
          aws eks update-kubeconfig \
            --region ${{ env.AWS_REGION }} \
            --name ${{ needs.terraform-apply.outputs.eks_cluster_name }}
      - name: Install ArgoCD CLI
        run: |
          curl -sSL -o /usr/local/bin/argocd \
            https://github.com/argoproj/argo-cd/releases/latest/download/argocd-linux-amd64
          chmod +x /usr/local/bin/argocd

      # Wait for ArgoCD UI to be reachable before CLI commands
      - name: Wait for ArgoCD UI to respond
        run: |
          URL="${{ needs.install-argocd.outputs.argocd_url }}"
          for i in $(seq 1 20); do
            STATUS=$(curl -s -o /dev/null -w "%{http_code}" "$URL" --max-time 10 2>/dev/null || echo "000")
            if [ "$STATUS" = "200" ] || [ "$STATUS" = "307" ] || [ "$STATUS" = "302" ]; then
              echo "ArgoCD UI responding (HTTP $STATUS)"; break
            fi
            echo "Attempt $i/20: HTTP $STATUS — waiting 15s..."
            sleep 15
          done

      # Login using the public LoadBalancer URL — no port-forward needed
      - name: Login to ArgoCD via LoadBalancer
        run: |
          ARGOCD_HOST=$(echo "${{ needs.install-argocd.outputs.argocd_url }}" | sed 's|http://||')
          argocd login "$ARGOCD_HOST" \
            --username admin \
            --password "${{ needs.install-argocd.outputs.argocd_password }}" \
            --insecure --grpc-web
          echo "Logged into ArgoCD at $ARGOCD_HOST"

      # Register YOUR specific repo so ArgoCD can watch it
      # Requires GH_PAT secret: GitHub → Settings → Developer settings
      # → Personal access tokens → Classic → repo scope
      - name: Register parthorookie/ecommercethreetierevent1edited in ArgoCD
        run: |
          ARGOCD_HOST=$(echo "${{ needs.install-argocd.outputs.argocd_url }}" | sed 's|http://||')
          argocd repo add ${{ env.GITHUB_REPO }} \
            --server "$ARGOCD_HOST" --insecure \
            --username parthorookie \
            --password "${{ secrets.GH_PAT }}" \
            --upsert
          echo "Repo registered in ArgoCD"

      # App 1: backend + frontend
      # Watches: helm/ecommerce/ in parthorookie/ecommercethreetierevent1edited
      # Any push to main that changes helm/ecommerce/values.yaml
      # (e.g. new image tag from CI) → ArgoCD auto-deploys to EKS
      - name: Create ArgoCD App — ecommerce-backend
        run: |
          ARGOCD_HOST=$(echo "${{ needs.install-argocd.outputs.argocd_url }}" | sed 's|http://||')
          argocd app create ecommerce-backend \
            --server "$ARGOCD_HOST" --insecure \
            --repo "${{ env.GITHUB_REPO }}" \
            --path "helm/ecommerce" \
            --dest-server "https://kubernetes.default.svc" \
            --dest-namespace default \
            --revision HEAD \
            --sync-policy automated \
            --auto-prune \
            --self-heal \
            --helm-set "backend.image.repository=${{ needs.terraform-apply.outputs.ecr_backend_url }}" \
            --helm-set "frontend.image.repository=${{ needs.terraform-apply.outputs.ecr_backend_url }}" \
            --upsert
          echo "ArgoCD App ecommerce-backend created"

      # App 2: worker
      # Watches: helm/worker/ in parthorookie/ecommercethreetierevent1edited
      - name: Create ArgoCD App — ecommerce-worker
        run: |
          ARGOCD_HOST=$(echo "${{ needs.install-argocd.outputs.argocd_url }}" | sed 's|http://||')
          argocd app create ecommerce-worker \
            --server "$ARGOCD_HOST" --insecure \
            --repo "${{ env.GITHUB_REPO }}" \
            --path "helm/worker" \
            --dest-server "https://kubernetes.default.svc" \
            --dest-namespace worker \
            --revision HEAD \
            --sync-policy automated \
            --auto-prune \
            --self-heal \
            --helm-set "image.repository=${{ needs.terraform-apply.outputs.ecr_worker_url }}" \
            --upsert
          echo "ArgoCD App ecommerce-worker created"

      - name: Trigger initial sync
        run: |
          ARGOCD_HOST=$(echo "${{ needs.install-argocd.outputs.argocd_url }}" | sed 's|http://||')
          argocd app sync ecommerce-backend --server "$ARGOCD_HOST" --insecure --async
          argocd app sync ecommerce-worker  --server "$ARGOCD_HOST" --insecure --async

      - name: List ArgoCD apps
        run: |
          ARGOCD_HOST=$(echo "${{ needs.install-argocd.outputs.argocd_url }}" | sed 's|http://||')
          argocd app list --server "$ARGOCD_HOST" --insecure

  # ═══════════════════════════════════════════════════════════════════════════
  # JOB 6 — Secrets + KEDA ScaledObject + Argo CronWorkflow
  # ═══════════════════════════════════════════════════════════════════════════
  deploy-supporting-resources:
    name: Secrets + KEDA + Argo CronWorkflow
    runs-on: ubuntu-latest
    needs: [terraform-apply, configure-argocd]
    steps:
      - uses: actions/checkout@v4
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id:     ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region:            ${{ env.AWS_REGION }}
      - uses: azure/setup-kubectl@v3
      - name: Update kubeconfig
        run: |
          aws eks update-kubeconfig \
            --region ${{ env.AWS_REGION }} \
            --name ${{ needs.terraform-apply.outputs.eks_cluster_name }}

      - name: Create app-secrets — default namespace
        run: |
          kubectl create secret generic app-secrets \
            --from-literal=db-host="${{ needs.terraform-apply.outputs.aurora_endpoint }}" \
            --from-literal=db-password="${{ secrets.DB_PASSWORD }}" \
            --from-literal=rabbit-url="amqp://admin:${{ secrets.RABBITMQ_PASSWORD }}@${{ needs.terraform-apply.outputs.rabbitmq_ip }}:5672" \
            --namespace default --dry-run=client -o yaml | kubectl apply -f -

      - name: Create app-secrets — worker namespace
        run: |
          kubectl create secret generic app-secrets \
            --from-literal=db-host="${{ needs.terraform-apply.outputs.aurora_endpoint }}" \
            --from-literal=db-password="${{ secrets.DB_PASSWORD }}" \
            --from-literal=rabbit-url="amqp://admin:${{ secrets.RABBITMQ_PASSWORD }}@${{ needs.terraform-apply.outputs.rabbitmq_ip }}:5672" \
            --namespace worker --dry-run=client -o yaml | kubectl apply -f -

      - name: Create rabbitmq-secret — worker namespace (KEDA)
        run: |
          kubectl create secret generic rabbitmq-secret \
            --from-literal=rabbit-url="amqp://admin:${{ secrets.RABBITMQ_PASSWORD }}@${{ needs.terraform-apply.outputs.rabbitmq_ip }}:5672" \
            --namespace worker --dry-run=client -o yaml | kubectl apply -f -

      - name: Create rabbitmq-secret — argo namespace (CronWorkflow)
        run: |
          kubectl create secret generic rabbitmq-secret \
            --from-literal=host="${{ needs.terraform-apply.outputs.rabbitmq_ip }}" \
            --from-literal=username="admin" \
            --from-literal=password="${{ secrets.RABBITMQ_PASSWORD }}" \
            --from-literal=rabbit-url="amqp://admin:${{ secrets.RABBITMQ_PASSWORD }}@${{ needs.terraform-apply.outputs.rabbitmq_ip }}:5672" \
            --namespace argo --dry-run=client -o yaml | kubectl apply -f -

      - name: Apply KEDA ScaledObject
        run: |
          kubectl apply -f k8s/keda-scaledobject.yaml
          kubectl get scaledobject -n worker

      - name: Apply Argo CronWorkflow (DLQ reprocessor every 5 min)
        run: |
          kubectl apply -f argo/dlq-cronworkflow.yaml
          kubectl get cronworkflow -n argo

  # ═══════════════════════════════════════════════════════════════════════════
  # JOB 7 — Wait for ArgoCD sync + verify all services on EKS
  # ═══════════════════════════════════════════════════════════════════════════
  verify-deployment:
    name: Verify ArgoCD Sync + EKS Health
    runs-on: ubuntu-latest
    needs: [terraform-apply, install-argocd, configure-argocd, deploy-supporting-resources]
    steps:
      - uses: actions/checkout@v4
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id:     ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region:            ${{ env.AWS_REGION }}
      - uses: azure/setup-kubectl@v3
      - name: Update kubeconfig
        run: |
          aws eks update-kubeconfig \
            --region ${{ env.AWS_REGION }} \
            --name ${{ needs.terraform-apply.outputs.eks_cluster_name }}
      - name: Install ArgoCD CLI
        run: |
          curl -sSL -o /usr/local/bin/argocd \
            https://github.com/argoproj/argo-cd/releases/latest/download/argocd-linux-amd64
          chmod +x /usr/local/bin/argocd
      - name: Login to ArgoCD
        run: |
          ARGOCD_HOST=$(echo "${{ needs.install-argocd.outputs.argocd_url }}" | sed 's|http://||')
          argocd login "$ARGOCD_HOST" \
            --username admin \
            --password "${{ needs.install-argocd.outputs.argocd_password }}" \
            --insecure --grpc-web

      # Wait until ArgoCD finishes syncing both apps to EKS
      - name: Wait for ArgoCD sync — ecommerce-backend
        run: |
          ARGOCD_HOST=$(echo "${{ needs.install-argocd.outputs.argocd_url }}" | sed 's|http://||')
          argocd app wait ecommerce-backend \
            --server "$ARGOCD_HOST" --insecure \
            --sync --health --timeout 300
          echo "ecommerce-backend: Synced + Healthy on EKS"

      - name: Wait for ArgoCD sync — ecommerce-worker
        run: |
          ARGOCD_HOST=$(echo "${{ needs.install-argocd.outputs.argocd_url }}" | sed 's|http://||')
          argocd app wait ecommerce-worker \
            --server "$ARGOCD_HOST" --insecure \
            --sync --health --timeout 300
          echo "ecommerce-worker: Synced + Healthy on EKS"

      - name: Verify all EKS resources
        run: |
          echo "=== Nodes ===" && kubectl get nodes
          echo "=== All pods ===" && kubectl get pods -A
          echo "=== LoadBalancer services ===" && kubectl get svc -A --field-selector spec.type=LoadBalancer
          echo "=== KEDA ScaledObject ===" && kubectl get scaledobject -n worker
          echo "=== Argo CronWorkflow ===" && kubectl get cronworkflow -n argo
          ARGOCD_HOST=$(echo "${{ needs.install-argocd.outputs.argocd_url }}" | sed 's|http://||')
          echo "=== ArgoCD apps ===" && argocd app list --server "$ARGOCD_HOST" --insecure

      - name: Health check — app via ALB
        run: |
          ALB="${{ needs.terraform-apply.outputs.alb_dns_name }}"
          for i in $(seq 1 18); do
            STATUS=$(curl -s -o /dev/null -w "%{http_code}" "http://$ALB/health" --max-time 10 2>/dev/null || echo "000")
            [ "$STATUS" = "200" ] && echo "App healthy at http://$ALB" && break
            echo "Attempt $i/18: HTTP $STATUS — waiting 10s..." && sleep 10
          done

      - name: Print all access URLs
        run: |
          ARGO_WF=$(kubectl get svc argo-workflows-server -n argo \
            -o jsonpath='{.status.loadBalancer.ingress[0].hostname}' 2>/dev/null || echo "pending")
          GRAFANA=$(kubectl get svc prometheus-grafana -n monitoring \
            -o jsonpath='{.status.loadBalancer.ingress[0].hostname}' 2>/dev/null || echo "pending")
          echo "========================================================"
          echo " DEPLOYMENT COMPLETE"
          echo "========================================================"
          echo " App (ALB)        : http://${{ needs.terraform-apply.outputs.alb_dns_name }}"
          echo " ArgoCD (NLB)     : ${{ needs.install-argocd.outputs.argocd_url }}"
          echo "   Username       : admin"
          echo "   Password       : ${{ needs.install-argocd.outputs.argocd_password }}"
          echo " Argo Workflows   : http://$ARGO_WF"
          echo " Grafana (NLB)    : http://$GRAFANA  (admin / ${{ secrets.GRAFANA_PASSWORD }})"
          echo "========================================================"
          echo " GitOps: push to main -> ArgoCD auto-syncs to EKS"
          echo " Repo: https://github.com/parthorookie/ecommercethreetierevent1edited"
          echo "========================================================"