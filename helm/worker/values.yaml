replicaCount: 2

image:
  repository: ""   # set by CI
  tag: "latest"
  pullPolicy: Always

resources:
  requests:
    cpu: "100m"
    memory: "128Mi"
  limits:
    cpu: "500m"
    memory: "512Mi"

env:
  NODE_ENV: production
  MAX_RETRIES: "5"

secrets:
  name: app-secrets
